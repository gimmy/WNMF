PROGRAM wnmf

  IMPLICIT NONE

  ! CALL read ( 'img_ascii.pgm' )
  CALL createA ()
  ! CALL factor (A, W, row, col, rank)

!  Terminate.

  ! WRITE ( *, '(a)' ) ' '
  ! CALL timestamp ( )

  ! stop

END PROGRAM wnmf
